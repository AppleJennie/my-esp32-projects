# 待确认清单（2026-08-26 凌晨，通宵任务产出）

> 以下事项涉及架构变更 / 对外接口 / 付费 / 需要你本人操作，按红线未擅自执行。每项附方案与工作量。

## 1. 深夜电台"推送到设备播放"（生成部分已完成）

**现状**：`~/esp/project/radio/` 已能定时生成沈星回人设 + 克隆音色的电台 mp3（已实测一次成功），cron 开关就绪。
**卡点**：设备空闲时**不保持连接**（唤醒才连 WebSocket），服务端在最简化部署下没有推送通道。两个方案：

- **方案 A：部署官方 MQTT 网关**（推荐）。服务端 `server.mqtt_gateway` 配置项就是干这个的，设备 OTA 时会拿到 MQTT 配置改为长连接。工作量：部署 mqtt-gateway 容器（官方有镜像）+ UDP 网关，约半天；**风险：2C2G 内存更紧，需把容器限额重新分配**。
- **方案 B：改固件保持长连接 + 服务端加推送接口**。改动大、影响 OTA 兼容，不建议。

## 2. 定时提醒/闹钟（"明早7点叫我"）

与第 1 条同一个卡点：服务端无法主动触达空闲设备。额外路径：

- **方案 A**：同 MQTT 网关（一次解决两个问题）。
- **方案 C：设备端 MCP 闹钟工具**（改固件）：新增 `self.alarm.set` MCP 工具，ESP32 本地定时到点自启播报。不依赖网关，但要改固件并重新烧录，工作量约 1 天。

## 3. 容器 unhealthy 自动重启（autoheal）

**现状**：compose 已加 healthcheck（OTA 接口 60s 探测），`docker ps` 可见 healthy 状态；`restart: always` 覆盖进程崩溃。
**待确认**：是否部署 `willfarrell/autoheal` 容器实现"unhealthy 自动重启"。新增一个常驻容器（约 10MB 内存），需要拉取新镜像。收益边际，未擅自加。

## 4. GLM 免费档限流（429）

回归测试多次触发 GLM 429（速率限制），晴天影响：用户连续快速对话可能偶发"卡一下"。
**选项**：① 接受现状（免费）；② 智谱充值提额；③ 换 DeepSeek 等低价 key。需要你决策。

## 5. 天气插件 key

模板已备好（仓库 `deploy/.config.example.yaml` 的 plugins 段，默认城市太原）。你去 https://console.qweather.com/#/apps/create-key/over 免费申请 key 发我，一分钟配上。
**过渡措施（已做）**：见工作汇报——已从启用插件列表移除 get_weather，避免"插件失败+二次 LLM"造成 43 秒延迟。

## 6. 服务器密码与火山 Key 重置

凭据在对话中暴露过，这个我替不了你：腾讯云控制台重置 SSH 密码；火山控制台轮换 Access Token / API Key（轮换后把新值发我更新配置，重启 1 分钟）。
