# 交接文档 — my-esp32-projects 仓库

> 本文档记录：这个仓库是什么、电脑里相关文件都放在哪、助手改动过什么、以后怎么维护。
> 建立日期：2026-08-24

---

## 一、这个仓库是什么

一个本地 git 仓库，收纳三个 ESP32 相关项目，准备推送到 GitHub 上的**一个**公开仓库（一个仓库装全部）。

仓库位置：`~/my-esp32-projects`（即 `/Users/applejennie/my-esp32-projects`）

## 二、目录结构

```
my-esp32-projects/
├── README.md                ← 仓库简介
├── 交接文档.md              ← 本文档
├── .gitignore               ← 排除规则（编译产物不进仓库）
├── .git/                    ← git 版本历史（隐藏目录，千万别删）
│
├── OV-Watch/                ← 智能手表（开源项目存档，保留原作者 LICENSE）
│   ├── Hardware/               硬件原理图 / PCB
│   ├── Firmware/               手表固件（源码 + hex/bin）
│   ├── Software/               上位机软件
│   ├── 3D Modle/               外壳 3D 模型
│   └── images/                 图片资料
│
├── xiaozhi-sleep-monitor/   ← 小智 ESP32 二次开发：睡眠健康监测
│   ├── main/                   主程序（含你的睡眠监测改动）
│   ├── docs/                   文档
│   ├── BUGFIX_LOG.md           你的 bug 修复日志
│   ├── 答辩材料.md 等           你的项目材料（报告、鼾声枕头资料）
│   └── sdkconfig 等            ESP-IDF 工程标准文件
│
└── moji2-firmware/          ← Moji 2.0 桌面机器人自研固件（ESP32-C5）
    ├── main/                   全部自研代码（圆屏 ST77916 / 音频 / WiFi / 电源）
    └── README.md               引脚定义与硬件说明
```

三个项目的搬家对照表（**旧路径已不存在**）：

| 原位置 | 现位置 |
|---|---|
| `~/Downloads/OV-Watch-main` | `~/my-esp32-projects/OV-Watch` |
| `~/Downloads/xiaozhi-esp32-main` | `~/my-esp32-projects/xiaozhi-sleep-monitor` |
| `~/esp32` | `~/my-esp32-projects/moji2-firmware` |

⚠️ 以后开发/编译 Moji 固件请在新路径 `~/my-esp32-projects/moji2-firmware` 下进行；第一次编译前先 `idf.py fullclean`（旧 build 缓存里记着老路径）。

未收入本仓库：`~/Downloads` 里还有两个立创EDA 工程文件（`ProPrj_智能手表OV-Watch_2026-07-15.epro` 等），如需收入再告知。

## 三、电脑里相关文件都在哪

| 位置 | 是什么 | 备注 |
|---|---|---|
| `~/my-esp32-projects/` | 仓库本体（代码 + .git 历史） | 日常工作目录 |
| `~/.ssh/id_ed25519` | SSH **私钥** | 🔒 机密，永远不发给任何人、不贴进聊天 |
| `~/.ssh/id_ed25519.pub` | SSH **公钥** | 可公开；贴到 GitHub 的就是它 |
| `~/.ssh/config` | SSH 配置 | 让 github.com 走 Clash 代理（127.0.0.1:17891） |
| `~/.local/bin/gh` | GitHub 官方命令行工具 | 已安装；本次用 SSH 方案，暂用不到 |
| 仓库内 git 配置 | `user.name=applejennie`<br>`user.email=applejennie@users.noreply.github.com` | 只对本仓库生效；想改邮箱：`git config user.email "你的邮箱"` |

本仓库的公钥全文（贴公钥用这串）：

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJaMIBZVieJYGqkBkBraKvMaRTO+9gZ8Br5G+DJlNrOG applejennie@mac
```

查看方式：`cat ~/.ssh/id_ed25519.pub`

## 四、上传 GitHub 进度清单

已完成（助手操作）：

- [x] 三个项目归拢到一个目录并重命名，结构清晰
- [x] 配置 .gitignore（`build/`、`managed_components/` 等约 900MB 编译产物已排除）
- [x] git 初始提交（4478 个文件，提交号 7064190）
- [x] 生成 SSH 密钥对，代理通道测试通过

待你操作（浏览器，两步）：

- [ ] 贴公钥：打开 <https://github.com/settings/keys> → **New SSH key** → Title 填 `我的Mac` → 粘贴上面那串公钥 → Add
- [ ] 建空仓库：打开 <https://github.com/new> → 名字 `my-esp32-projects` → Public → **不勾** "Add a README" → Create
- [ ] 把你的 **GitHub 用户名**告诉助手，由助手完成验证与推送

## 五、以后日常怎么用

改完代码后，在 `~/my-esp32-projects` 里三连：

```bash
git add -A                      # 把所有改动加入暂存
git commit -m "说清楚这次改了什么"
git push                        # 推送到 GitHub
```

其他常用命令：

- `git status` —— 看哪些文件有改动
- `git log --oneline` —— 看提交历史
- 换新电脑：新电脑生成自己的 SSH 钥匙贴到 GitHub，然后
  `git clone git@github.com:用户名/my-esp32-projects.git`

## 六、注意事项

1. **私钥保密**：`~/.ssh/id_ed25519`（不带 .pub 的那个）是机密，任何人要都不给。
2. **推送依赖 Clash 代理**：`~/.ssh/config` 里配置了走 127.0.0.1:17891。若哪天不开代理也能直连 GitHub，删掉该文件中 `ProxyCommand` 那一行即可。
3. **编译产物不进仓库**：`build/`、`managed_components/` 已被 .gitignore 排除；换电脑克隆后用 ESP-IDF 重新编译、下载组件即可。
4. **GitHub 单文件上限 100MB**，超大文件（视频、压缩包）不要直接提交。
